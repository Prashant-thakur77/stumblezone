// The scene's one Hype instance. Every client counts the same cheers off the same bus, so the
// crowd goes wild for everybody at the same moment without a server deciding it.

import { Hype } from '../lib/hype'

export const hype = new Hype()
