## What this changes

## Why

## Checklist
- [ ] `npm run verify` is green
- [ ] Played it on a phone (model: )
- [ ] New logic lives in `src/lib/` with a test
- [ ] No `borderRadius`, no emoji, every `Label` sized
- [ ] `CHANGELOG.md` updated if a player would notice
